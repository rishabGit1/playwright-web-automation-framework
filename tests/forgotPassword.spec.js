const { test, expect } = require('@playwright/test');

test.describe('Forget Password Page Tests', () => {
    test('should verify forget password page functionality', async ({ page }) => {
        // Add your test code here
    });
});
